function solution(string) {
  var arr = string.split("");
  var result = [];
  for(var i=0;i<arr.length;i++){
    if (arr[i] == arr[i].toUpperCase()){
      result.push(" ");
      result.push(arr[i]);
    }
    else{
      result.push(arr[i]);
    }
  }
  return result.join("");
}
